# URLShield
Analizador de seguridad de URLs que revisa características potencialmente sospechosas, como acortadores, direcciones IP utilizadas como host, credenciales embebidas, caracteres CR/LF y URLs excesivamente largas. No visita la URL, por lo que el análisis es loca
